#include<iostream>
using namespace std;

int main()
{
  int a, b, c, avg;
  cout<<"Enter the value of a , b and c : ";
  cin >> a >> b >> c;
  avg = (a + b + c)/3;
  cout<<"The average is : "<< avg;
}
